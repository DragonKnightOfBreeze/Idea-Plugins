sys_anchorage = {
	potential = {
		exists = owner
		exists = starbase
		starbase = {
			has_starbase_size >= starbase_starport
			count_starbase_modules = {
				type = anchorage
				count > 1
			}
		}
	}

	weight_modifier = {
		base = 100	
		modifier = {
			add = 100
			starbase = { 
				count_starbase_modules = {
					type = anchorage
					count > 2
				} 
			}
		}
		modifier = {
			add = 100
			starbase = { 
				count_starbase_modules = {
					type = anchorage
					count > 3
				} 
			}
		}
		modifier = {
			add = 100
			starbase = { 
				count_starbase_modules = {
					type = anchorage
					count > 4
				} 
			}
		}	
		modifier = {
			add = 100
			starbase = { 
				count_starbase_modules = {
					type = anchorage
					count > 5
				} 
			}
		}
	}
}
