sys_mining = {
	potential = {
		exists = owner
	}

	weight_modifier = {
		modifier = {
			add = 1
			count_system_ships = {
				limit = { is_ship_size = mining_station }
				count > 1
			}			
		}	
		modifier = {
			add = 1
			count_system_ships = {
				limit = { is_ship_size = mining_station }
				count > 2
			}			
		}	
		modifier = {
			add = 1
			count_system_ships = {
				limit = { is_ship_size = mining_station }
				count > 3
			}			
		}	
		modifier = {
			add = 1
			count_system_ships = {
				limit = { is_ship_size = mining_station }
				count > 4
			}			
		}		
	}	
}
