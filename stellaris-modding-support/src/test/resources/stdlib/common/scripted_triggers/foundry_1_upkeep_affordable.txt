foundry_1_upkeep_affordable = {
	has_monthly_income = {
		resource = energy
		value > 4
	}
	OR = {
		AND = {
			is_hive_empire = yes
			has_monthly_income = {
				resource = minerals
				value > 12
			}
		}
		AND = {
			is_machine_empire = yes
			has_monthly_income = {
				resource = minerals
				value > 16
			}
		}
		AND = {
			is_regular_empire = yes
			has_monthly_income = {
				resource = minerals
				value > 12
			}
		}
	}
}
