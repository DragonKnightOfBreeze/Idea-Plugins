action_cancel_subject_integration = {
	select = {
		01_CANCEL_INTEGRATE_FRIENDLY = {
			trigger = {
				OR = {
					is_friendly_to = FROM
					is_cordial_to = FROM
					is_protective_to = FROM
				}
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_NEUTRAL = {
			trigger = {
				OR = {
					is_neutral_to = FROM
					is_threatened_to = FROM
				}
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_HOSTILE = {
			trigger = {
				OR = {
					is_hostile_to = FROM
					is_domineering_to = FROM
					is_unfriendly_to = FROM
				}
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_DISMISSIVE = {
			trigger = {
				is_dismissive_to = FROM
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_PATRONIZING = {
			trigger = {
				is_patronizing_to = FROM
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_ANGRY = {
			trigger = {
				is_angry_to = FROM
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		MECH_CANCEL_INTEGRATE_01 = {
			trigger = {
				has_authority = auth_machine_intelligence
			}
		}
		# Ketlings
		KET_CANCEL_INTEGRATE_01 = {
			weight = { weight = 100000 }
			trigger = {
				exists = event_target:ratling_country
				is_same_empire = event_target:ratling_country
			}
		}
		# Corporate
		CORP_CANCEL_INTEGRATE_01 = {
			trigger = {
				has_authority = auth_corporate
			}
		}
	}

	propose = {
		01_CANCEL_INTEGRATE_PROPOSE = {
			trigger = {}
		}
	}

	accept = {
		01_CANCEL_INTEGRATE_ACCEPT_FRIENDLY = {
			trigger = {
				OR = {
					is_friendly_to = FROM
					is_cordial_to = FROM
					is_protective_to = FROM
				}
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_ACCEPT_NEUTRAL = {
			trigger = {
				OR = {
					is_neutral_to = FROM
					is_threatened_to = FROM
				}
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_ACCEPT_HOSTILE = {
			trigger = {
				OR = {
					is_hostile_to = FROM
					is_domineering_to = FROM
					is_unfriendly_to = FROM
				}
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_ACCEPT_DISMISSIVE = {
			trigger = {
				is_dismissive_to = FROM
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_ACCEPT_PATRONIZING = {
			trigger = {
				is_patronizing_to = FROM
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		01_CANCEL_INTEGRATE_ACCEPT_ANGRY = {
			trigger = {
				is_angry_to = FROM
				NOR = {
					has_authority = auth_machine_intelligence
					has_authority = auth_corporate
				}
			}
		}
		MECH_CANCEL_INTEGRATE_ACCEPT_FRIENDLY_01 = {
			trigger = {
				has_authority = auth_machine_intelligence
				OR = {
					is_friendly_to = FROM
					is_cordial_to = FROM
					is_protective_to = FROM
				}
			}
		}
		MECH_CANCEL_INTEGRATE_ACCEPT_NEUTRAL_01 = {
			trigger = {
				has_authority = auth_machine_intelligence
				OR = {
					is_neutral_to = FROM
					is_threatened_to = FROM
					is_patronizing_to = FROM
				}
			}
		}
		MECH_CANCEL_INTEGRATE_ACCEPT_HOSTILE_01 = {
			trigger = {
				has_authority = auth_machine_intelligence
				OR = {
					is_angry_to = FROM
					is_dismissive_to = FROM
					is_hostile_to = FROM
					is_domineering_to = FROM
					is_unfriendly_to = FROM
				}
			}
		}
		# Ketlings
		KET_CANCEL_INTEGRATE_ACCEPT_01 = {
			weight = { weight = 100000 }
			trigger = {
				exists = event_target:ratling_country
				is_same_empire = event_target:ratling_country
			}
		}
		# Corporate
		CORP_CANCEL_INTEGRATE_ACCEPT_01 = {
			trigger = {
				has_authority = auth_corporate
			}
		}
	}
}
