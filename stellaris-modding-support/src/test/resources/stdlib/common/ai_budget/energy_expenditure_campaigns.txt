energy_expenditure_campaigns = {
	resource = energy 
	type = expenditure
	category = campaigns
	
	potential = {
		# only consider campaigns when we can actually afford them
		has_resource = { 
			type = energy 
			amount > 3000
		}
	}
	
	fraction = {
		weight = 0.30 
	}

	static_min = {
		base = 1000
	}
	static_max = {
		base = 3000
	}
}
