### 宣战理由

title: Casus Belli
type: object
additionalProperties:
  $ref: "#/definitions/casus_belli"

definitions:
  casus_belli:
    title: Casus Belli
    type: object
    properties:
      show_in_diplomacy:
        type: boolean
        default: yes
      show_notification:
        type: boolean
        default: yes
      potential:
        $ref: "../core.yml#/definitions/triggers"
      is_valid:
        $ref: "../core.yml#/definitions/triggers"
      destroy_if:
        $ref: "../core.yml#/definitions/triggers"
