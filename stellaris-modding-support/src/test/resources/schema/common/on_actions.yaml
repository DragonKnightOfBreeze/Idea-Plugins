definitions:
  # 动作
  on_action:
    type: object
    properties:
      events:
        type: array
        items:
          $type: event.id

type: object
additionalProperties:
  $ref: "#/definitions/on_action"
