definitions:
  # 随机名字
  random_name:
    type: object
    properties:
      size:
        type: integer
      possible:
        $ref: "../core.yml#/definitions/triggers"

type: object
additionalProperties:
  $ref: "#/definitions/random_name"
