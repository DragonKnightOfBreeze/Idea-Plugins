### 地块类型

title: Deposit Categories
type: object
additionalProperties:
  $ref: "#/definitions/deposit_category"

definitions:
  deposit_category:
    title: Deposit Category
    type: object
    properties:
      blocker:
        type: boolean
        optional: true
      important:
        type: boolean
        optional: true
