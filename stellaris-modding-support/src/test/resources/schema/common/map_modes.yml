definitions:
  # 地图模式
  map_mode:
    type: object
    properties:
      icon:
        $ref: "../core.yml#/definitions/gfx_reference"
      hardcoded_tooltip:
        $type: opinion
      can_change_point_of_view:
        $ref: "../core.yml#/definitions/boolean"
      enable_terra_incognita:
        $ref: "../core.yml#/definitions/boolean"
      shortcut:
        $ref: "../core.yml#/definitions/shortcut"
      color:
        $ref: "#/definitions/color"
        multiple: true
  color:
    type: object
    properties:
      value:
        $ref: "../core.yml#/definitions/color_type"
      custom_tooltip:
        $ref: "../core.yml#/definitions/loc_reference"
      hardcoded_tooltip:
        type: string
      condition:
        $ref: "../core.yml#/definitions/triggers"

type: object
additionalProperties:
  $ref: "#/definitions/map_mode"
