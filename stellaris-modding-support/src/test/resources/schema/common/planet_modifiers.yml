definitions:
  # 星球修饰符
  plant_modifier:
    type: object
    properties:
      spawn_chance:
        $ref: "../core.yml#/definitions/modifier_chance"
      modifier:
        $type: modifier

type: object
additionalProperties:
  $ref: "#/definitions/plant_modifier"
