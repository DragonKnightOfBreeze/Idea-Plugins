### 联邦法律分类

title: Federation Law Categories
type: object
additionalProperties:
  $ref: "#/definitions/federation_law_category"

definitions:
  federation_law_category:
    title: Federation Law Category
    type: object
    properties:
      centralization:
        type: boolean
      show_succession_date:
        type: boolean
      laws:
        type: array
        items:
          $type: federation_law
      potential:
        $ref: "../../core.yml#/definitions/triggers"
