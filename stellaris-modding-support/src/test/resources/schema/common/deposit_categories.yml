definitions:
  # 地块类型
  deposit_category:
    type: object
    properties:
      blocker:
        $ref: "../core.yml#/definitions/boolean"
        optional: true
      important:
        $ref: "../core.yml#/definitions/boolean"
        optional: true

type: object
additionalProperties:
  $ref: "#/definitions/deposit_category"


