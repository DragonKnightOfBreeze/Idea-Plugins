definitions:
  # 建筑
  button_effect:
    type: object
    properties:
      potential:
        $ref: "../core.yml#/definitions/triggers"
      allow:
        $ref: "../core.yml#/definitions/triggers"
      effect:
        $ref: "../core.yml#/definitions/effects"

type: object
additionalProperties:
  $ref: "#/definitions/button_effect"
