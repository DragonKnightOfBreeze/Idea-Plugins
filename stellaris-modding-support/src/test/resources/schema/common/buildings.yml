definitions:
  # 建筑
  building:
    type: object
    properties:
      base_buildtime:
        $ref: "../core.yml#/definitions/int"
      base_cap_amount:
        $ref: "../core.yml#/definitions/int"
        optional: true
      capital:
        $ref: "../core.yml#/definitions/boolean"
      can_build:
        $ref: "../core.yml#/definitions/boolean"
      can_demolish:
        $ref: "../core.yml#/definitions/boolean"
      can_be_ruined:
        $ref: "../core.yml#/definitions/boolean"
      can_be_disabled:
        $ref: "../core.yml#/definitions/boolean"
      category:
        $ref: "#/definitions/building_category"
      potential:
        $ref: "../core.yml#/definitions/triggers"
      convert_to:
        type: array
        items:
          $type: building
      planet_modifier:
        $ref: "../core.yml#/definitions/modifier"
      destroy_trigger:
        $ref: "../core.yml#/definitions/triggers"
      allow:
        $ref: "../core.yml#/definitions/triggers"
      # anyOf
      triggered_plant_modifier:
        $ref: "../core.yml#/definitions/triggered_modifier"
      resources:
        $ref: "../core.yml#/definitions/economy_unit"
      upgrades:
        type: array
        items:
          $type: building
      prerequisites:
        type: array
        items:
          $type: building
      triggered_desc:
        $ref: "../core.yml#/definitions/triggered_desc"
        multiple: true
      on_queued:
        $ref: "../core.yml#/definitions/effects"
      on_unqueued:
        $ref: "../core.yml#/definitions/effects"
      on_built:
        $ref: "../core.yml#/definitions/effects"
      on_destroy:
        $ref: "../core.yml#/definitions/effects"
      ai_weight:
        $ref: "../core.yml#/definitions/weight_modifier"
  building_category:
    type: string
    enum:
    - government
    - planet_buildings
    - pop_assembly
    - resource
    - manufacturing
    - research
    - trade
    - amenity
    - unity
    - army

type: object
additionalProperties:
  $ref: "#/definitions/building"
