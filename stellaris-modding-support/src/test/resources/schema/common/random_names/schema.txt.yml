### 随机名称

title: Random Names
type: object
additionalProperties:
  $ref: "#/definitions/random_name"

definitions:
  random_name:
    title: Random Name
    type: object
    properties:
      size:
        type: integer
      possible:
        $ref: "../../core.yml#/definitions/triggers"
