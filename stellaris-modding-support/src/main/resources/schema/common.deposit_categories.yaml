definitions:
  # 地块类型
  deposit_category:
    type: object
    properties:
      # opt
      blocker:
        $ref: "core.yaml/#/definitions/boolean"
      # opt
      important:
        $ref: "core.yaml/#/definitions/boolean"

type: object
additionalProperties:
  $ref: "#/definitions/deposit_category"


