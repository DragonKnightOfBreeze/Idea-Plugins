definitions:
  # 小行星带
  asteroid_belt:
    type: object
    properties:
      # anyOf
      mesh:
        $ref: "../core.yaml/#/definitions/gfx_reference"
      shader:
        type: string

type: object
additionalProperties:
  $ref: "#/definitions/asteroid_belts"
