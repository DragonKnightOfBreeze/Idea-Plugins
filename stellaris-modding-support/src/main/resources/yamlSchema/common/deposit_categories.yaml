definitions:
  # 地块类型
  deposit_category:
    type: object
    properties:
      blocker:
        $ref: "../core.yaml/#/definitions/boolean"
        optional: true
      important:
        $ref: "../core.yaml/#/definitions/boolean"
        optional: true

type: object
additionalProperties:
  $ref: "#/definitions/deposit_category"


